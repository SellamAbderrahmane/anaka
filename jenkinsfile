pipeline {
    agent any
    environment {
        CI = 'true'
    }
     stages {
        stage("Build") {
            steps {
                dat "npm install"
                dat "npm run build"
            }
        }
        stage("Deploy") {
            steps {
                dat "npm start"
            }
        }
    }
}
